server:
  port: 8083
  servlet:
    context-path: /posts
spring:
  application:
    name: post-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/post-service?authSource=admin
app:
  services:
    profile:
      url: http://localhost:8081/profile